function showAlert() {
    alert("This is an alert message by a js coding!");
}

function askName() { 
    const name = prompt("What is your name?");
    if (name) {
        alert("Hello, " + name + "!");
    }
}